import 'package:flutter/material.dart';

/// Named, semantic colors instead of every feature hard-coding hex
/// values — matches the palette used in the concept/architecture
/// diagrams so design and code stay in sync.
class AppColors {
  AppColors._();

  static const navy = Color(0xFF1F3864);
  static const accent = Color(0xFF2E74B5);
  static const success = Color(0xFF2E8B57);
  static const warning = Color(0xFFC97A1A);
  static const danger = Color(0xFFC0392B);
  static const purple = Color(0xFF7A52A8);
  static const neutralBg = Color(0xFFF4F7FA);
}
